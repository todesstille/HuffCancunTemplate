{
  "name": "hardhat-project",
  "scripts": {
    "compile": "cd huff/ && rm -f ./contract.hex && huffc contract.huff --bytecode >> contract.hex && cd .."
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.22.7"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
